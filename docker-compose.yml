services:

  minio:
    image: minio/minio
    ports:
      - '9000:9000'
      - '9001:9001'
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    command: server /data --console-address ':9001'

  postgres:
    image: postgres:latest
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - pgdata:/var/lib/postgresql/data/pgdata
    depends_on:
      - minio

  media-service:
    build:
      context: ./media-service
      dockerfile: Dockerfile
    container_name: media_service_app
    ports:
      - '8080:8080'
      - '8000:8000'
    environment:
      - MINIO_URL=http://minio:9000 
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
      - MINIO_BUCKET_NAME=${MINIO_BUCKET_NAME}
      - MINIO_MAX_FILE_SIZE=${MINIO_MAX_FILE_SIZE}
      - MINIO_COVER_ROOT=${MINIO_COVER_ROOT}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_HOST=${POSTGRES_HOST}  
      - POSTGRES_PORT=${POSTGRES_PORT}
      - POSTGRES_URL=${POSTGRES_URL}
      - TRACK_MAX_GET_SIZE=${TRACK_MAX_GET_SIZE}
      - TRACK_DEFAULT_GET_SIZE=${TRACK_DEFAULT_GET_SIZE}
    command: fastapi dev src/main.py --host 0.0.0.0 --port 8000
    depends_on:
      - minio
      - postgres
    volumes:
      - ./media-service:/app

volumes:
  pgdata: